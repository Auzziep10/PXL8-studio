
{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user of the PXL8 DTF Platform.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the User entity."
        },
        "email": {
          "type": "string",
          "description": "Email address of the user.",
          "format": "email"
        },
        "firstName": {
          "type": "string",
          "description": "First name of the user."
        },
        "lastName": {
          "type": "string",
          "description": "Last name of the user."
        },
        "createdAt": {
          "type": "string",
          "description": "Date and time the user account was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Date and time the user account was last updated.",
          "format": "date-time"
        },
        "address": {
            "type": "object",
            "description": "Default shipping address for the user.",
            "properties": {
                "street": { "type": "string" },
                "city": { "type": "string" },
                "state": { "type": "string" },
                "zip": { "type": "string" },
                "country": { "type": "string" }
            }
        }
      },
      "required": [
        "id",
        "email",
        "firstName",
        "lastName",
        "createdAt",
        "updatedAt"
      ]
    },
    "GangSheet": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "GangSheet",
      "type": "object",
      "description": "Represents a gang sheet created by a user.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the GangSheet entity."
        },
        "userId": {
          "type": "string",
          "description": "Reference to the User who created the gang sheet. (Relationship: User 1:N GangSheet)"
        },
        "name": {
          "type": "string",
          "description": "Name of the gang sheet (user-defined)."
        },
        "imageUrl": {
          "type": "string",
          "description": "URL of the gang sheet image stored in Firebase Storage."
        },
        "width": {
          "type": "number",
          "description": "Width of the gang sheet in inches."
        },
        "height": {
          "type": "number",
          "description": "Height of the gang sheet in inches."
        },
        "dpi": {
          "type": "number",
          "description": "DPI (dots per inch) of the gang sheet."
        },
        "createdAt": {
          "type": "string",
          "description": "Date and time the gang sheet was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Date and time the gang sheet was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "userId",
        "name",
        "imageUrl",
        "width",
        "height",
        "dpi",
        "createdAt",
        "updatedAt"
      ]
    },
    "Order": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Order",
      "type": "object",
      "description": "Represents an order placed by a user.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Order entity."
        },
        "userId": {
          "type": "string",
          "description": "Reference to the User who placed the order. (Relationship: User 1:N Order)"
        },
        "gangSheetIds": {
          "type": "array",
          "description": "References to the GangSheets included in the order. (Relationship: Order 1:N GangSheet)",
          "items": {
            "type": "string"
          }
        },
        "orderDate": {
          "type": "string",
          "description": "Date the order was placed.",
          "format": "date-time"
        },
        "status": {
          "type": "string",
          "description": "Current status of the order (e.g., Pending, Processing, Shipped, Delivered)."
        },
        "shippingAddress": {
          "type": "string",
          "description": "Shipping address for the order."
        },
        "trackingId": {
          "type": "string",
          "description": "Tracking ID for the order."
        },
        "createdAt": {
          "type": "string",
          "description": "Date and time the order was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Date and time the order was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "userId",
        "gangSheetIds",
        "orderDate",
        "status",
        "shippingAddress",
        "trackingId",
        "createdAt",
        "updatedAt"
      ]
    },
    "SheetSize": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "SheetSize",
      "type": "object",
      "description": "Represents a pricing tier for a gang sheet size.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the SheetSize entity."
        },
        "name": {
          "type": "string",
          "description": "Display name for the sheet size (e.g., Small, 22x24)."
        },
        "width": {
          "type": "number",
          "description": "Width of the sheet in inches."
        },
        "height": {
          "type": "number",
          "description": "Height of the sheet in inches."
        },
        "price": {
          "type": "number",
          "description": "Price for this sheet size in USD."
        },
        "usage": {
          "type": "string",
          "description": "Determines where this pricing tier is available.",
          "enum": ["Builder", "Upload"]
        },
        "discount": {
          "type": "number",
          "description": "A percentage discount to apply for this specific sheet size."
        }
      },
      "required": ["id", "name", "width", "height", "price", "usage", "discount"]
    },
    "ServiceAddOn": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ServiceAddOn",
      "type": "object",
      "description": "Represents a billable service add-on.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the ServiceAddOn entity."
        },
        "name": {
          "type": "string",
          "description": "Display name for the service (e.g., Rush Order)."
        },
        "description": {
          "type": "string",
          "description": "A brief explanation of what the service includes."
        },
        "price": {
          "type": "number",
          "description": "Price for this service in USD."
        },
        "type": {
          "type": "string",
          "description": "The type of service add-on, used for specific logic.",
          "enum": ["one_time_fee", "per_sq_inch", "wide_format_discount"]
        }
      },
      "required": ["id", "name", "description", "price"]
    },
    "UploadSession": {
        "$schema": "http://json-schema.org/draft-07/schema#",
        "title": "UploadSession",
        "type": "object",
        "description": "A temporary document to bridge a mobile upload session to a desktop session.",
        "properties": {
            "imageUrl": {
                "type": "string",
                "description": "The Firebase Storage URL of the uploaded image."
            },
            "fileName": {
                "type": "string",
                "description": "The original name of the uploaded file."
            },
            "uploadedAt": {
                "type": "string",
                "description": "Timestamp of when the upload occurred.",
                "format": "date-time"
            }
        },
        "required": ["imageUrl", "fileName", "uploadedAt"]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Stores user profile information.  Path-based ownership for private user data.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier for the user."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/gangSheets/{gangSheetId}",
        "definition": {
          "entityName": "GangSheet",
          "schema": {
            "$ref": "#/backend/entities/GangSheet"
          },
          "description": "Stores gang sheets created by users. Path-based ownership ensures only the user can access their gang sheets.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier for the user who created the gang sheet."
            },
            {
              "name": "gangSheetId",
              "description": "The unique identifier for the gang sheet."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/orders/{orderId}",
        "definition": {
          "entityName": "Order",
          "schema": {
            "$ref": "#/backend/entities/Order"
          },
          "description": "Stores orders placed by users. Path-based ownership ensures only the user can access their orders.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier for the user who placed the order."
            },
            {
              "name": "orderId",
              "description": "The unique identifier for the order."
            }
          ]
        }
      },
      {
        "path": "/sheetSizes/{sheetSizeId}",
        "definition": {
          "entityName": "SheetSize",
          "schema": {
            "$ref": "#/backend/entities/SheetSize"
          },
          "description": "Stores global pricing tiers for gang sheet sizes. Writable only by admins.",
          "params": [
            {
              "name": "sheetSizeId",
              "description": "The unique identifier for the sheet size."
            }
          ]
        }
      },
      {
        "path": "/serviceAddOns/{addOnId}",
        "definition": {
          "entityName": "ServiceAddOn",
          "schema": {
            "$ref": "#/backend/entities/ServiceAddOn"
          },
          "description": "Stores global service add-ons like 'Rush Order'. Writable only by admins.",
          "params": [
            {
              "name": "addOnId",
              "description": "The unique identifier for the service add-on."
            }
          ]
        }
      },
      {
        "path": "/upload_sessions/{sessionId}",
        "definition": {
          "entityName": "UploadSession",
          "schema": {
            "$ref": "#/backend/entities/UploadSession"
          },
          "description": "Stores temporary information for a mobile-to-desktop upload session, linked by the user's UID.",
          "params": [
            {
              "name": "sessionId",
              "description": "The user's UID, acting as a unique session identifier."
            }
          ]
        }
      }
    ],
    "reasoning": "The Firestore structure is designed to store user data, gang sheets, and orders in a way that prioritizes security, scalability, and ease of debugging. Authorization Independence is achieved by leveraging path-based ownership for gang sheets and orders, eliminating the need for complex `get()` calls in security rules. All `list` operations are secured by the path-based ownership, ensuring that users can only access their own data. This also adheres to the principle that \"Rules are not Filters\" (QAPs). The structure uses explicit state modeling via the 'status' field in the Order entity. Furthermore, the data model enforces radical consistency by utilizing semantic naming conventions for collections, documents, and fields (e.g., always `userId` for user references). The use of hierarchical paths for User-owned entities like GangSheets (`/users/{userId}/gangSheets/{gangSheetId}`) and Orders (`/users/{userId}/orders/{orderId}`) ensures that ownership-based security rules are simple and efficient. This structure facilitates secure `list` operations since rules can simply check `request.auth.uid == userId`. The new `/sheetSizes` collection is a global, root-level collection for pricing information, readable by all authenticated users but writable only by admins."
  }
}
